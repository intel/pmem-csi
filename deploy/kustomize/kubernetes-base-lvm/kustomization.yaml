# Turns generic Kubernetes 1.16 deployment into deployment for LVM mode.

bases:
- ../kubernetes-base/

commonLabels:
  pmem-csi.intel.com/deployment: lvm-production

patchesJson6902:
- target:
    group: apps
    version: v1
    kind: DaemonSet
    name: pmem-csi-node
  path: ../patches/lvm-patch.yaml
